services:
  - type: web
    name: pizzint-monitor-backend
    env: node
    plan: free
    region: oregon
    buildCommand: npm install && npx playwright install --with-deps chromium
    startCommand: node index.js
